#ifndef _GUI_ENUM_H
#define _GUI_ENUM_H

enum {
  VERTICES_NONE,
  VERTICES_SQUARED,
  VERTICES_ROUNDED,
};

enum {
  PROJECTION_PARALLEL,
  PROJECTION_CENTRAL,
};

enum {
  EDGE_SOLID,
  EDGE_DASHED,
};


#endif /** _GUI_ENUM_H **/
